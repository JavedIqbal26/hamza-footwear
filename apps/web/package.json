{
  "name": "@hamza/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "typecheck": "astro check",
    "db:migrate": "wrangler d1 migrations apply hamza-footwear-db --local --persist-to ../../.wrangler-local",
    "db:seed": "wrangler d1 execute hamza-footwear-db --local --persist-to ../../.wrangler-local --file=../../db/seed/0001_cities.sql && wrangler d1 execute hamza-footwear-db --local --persist-to ../../.wrangler-local --file=../../db/seed/0002_products.sql",
    "db:reset": "npm run db:migrate && npm run db:seed",
    "db:migrate:remote": "wrangler d1 migrations apply hamza-footwear-db --remote",
    "db:seed:remote": "wrangler d1 execute hamza-footwear-db --remote --file=../../db/seed/0001_cities.sql && wrangler d1 execute hamza-footwear-db --remote --file=../../db/seed/0002_products.sql"
  },
  "dependencies": {
    "@hamza/db": "*",
    "@hamza/shared": "*",
    "astro": "^5.1.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/cloudflare": "^12.2.0",
    "@cloudflare/workers-types": "^4.20241230.0",
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2"
  }
}
