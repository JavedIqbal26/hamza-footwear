---
import Icon from '../icons/Icon.astro';
import { TRUST } from '../../content/microcopy.js';

/**
 * The reassurances that decide whether a first-time visitor orders at all.
 *
 * Below the product grid on purpose: a visitor arriving from a TikTok video
 * wants to see shoes, not read three paragraphs first — and the Cash on
 * Delivery promise is already in the strip above the header on every page.
 *
 * The advance delivery charge rides in the muted body line rather than the
 * heading: disclosed on the home page, but not competing with the promise that
 * brought the visitor here.
 */
const POINTS = [
  { icon: 'rupee' as const, title: 'Cash on Delivery', body: TRUST.codAdvance },
  { icon: 'truck' as const, title: '2–4 din delivery', body: TRUST.deliveryTime },
  { icon: 'exchange' as const, title: '7-day return ya exchange', body: TRUST.returns },
  { icon: 'check' as const, title: 'Asli tasveerein', body: 'Jo tasveer mein hai wohi milega.' },
] as const;
---

<section aria-label="Why order from us" class="px-4 py-12 sm:px-6">
  <ul class="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">
    {
      POINTS.map((point) => (
        <li class="rounded-xl border border-line p-5">
          <Icon name={point.icon} size={22} class="text-brand-600" />
          <p class="mt-2.5 text-[15px] font-extrabold">{point.title}</p>
          <p class="mt-1 text-[13px] leading-relaxed text-ink-muted">{point.body}</p>
        </li>
      ))
    }
  </ul>
</section>
